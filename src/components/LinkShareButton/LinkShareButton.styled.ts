import styled from 'styled-components';
import { defaultButtonStyle } from '../../styles/global-styled-css';

export const LinkShareButton = styled.div`
  display: inline;
  button {
    margin: 0;
    ${defaultButtonStyle}
    font-size: 1rem;
  }
`;
