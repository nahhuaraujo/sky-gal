import styled from 'styled-components';
import { secondaryColorMid } from '../../styles/global-styled-css';

export const Card = styled.div`
  padding: 1rem;
  border: 1px solid ${secondaryColorMid};
  border-radius: 1rem;
`;
