import styled from 'styled-components';
import { secondaryFontSizeValue } from '../../styles/global-styled-css';

export const Nav = styled.nav`
  display: flex;
  justify-content: space-around;
  font-size: ${secondaryFontSizeValue};
`;
